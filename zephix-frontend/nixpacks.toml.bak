[providers]
node = "20"

[phases]
install = "npm ci"
build = "npm run build"
start = "npm run preview -- --host 0.0.0.0 --port $PORT"

[static]
disable = true

[env]
PORT = "8080"
NODE_ENV = "production"
