[phases.setup]
nixPkgs = ['nodejs_20', 'npm']

[phases.install]
cmds = ['npm install --omit=dev']

[phases.build]
cmds = ['npm run build']

[phases.start]
cmd = 'npm run preview -- --host 0.0.0.0 --port $PORT'

[variables]
NODE_ENV = 'production'
PORT = '3000'
