[providers]
node

[phases.setup]
nixPkgs = ['nodejs-20_x', 'npm-10_x']

[phases.install]
cmds = ['npm install --omit=dev']

[phases.build]
cmds = ['npm run build']

[phases.start]
cmd = 'npm run preview -- --host 0.0.0.0 --port $PORT'

[variables]
NODE_ENV = 'production'
PORT = '3000'
