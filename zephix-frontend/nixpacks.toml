providers = ["node"]

[variables]
NODE_VERSION = "20.19.0"
NODE_ENV = "production"

[phases.build]
cmds = ["npm ci --omit=dev", "npm run build"]

[phases.start]
cmd = "npm run preview -- --host 0.0.0.0 --port $PORT"