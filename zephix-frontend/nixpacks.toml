# nixpacks.toml - Configuration for Project Zephix Frontend

# Use "node" not "nodejs" - this is the correct provider name
providers = ["node"]

[phases.build]
cmd = "npm run build"

[start]
cmd = "npm run preview -- --host 0.0.0.0"
