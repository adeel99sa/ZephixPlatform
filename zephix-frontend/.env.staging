# Copy from docs/ai/environments/staging.env (STAGING_BACKEND_API). Must match Railway
# backend **Networking → Public URL** + `/api`. Avoid `…-staging-staging…` hostname typos
# (double segment) unless that URL is genuinely what Railway issued.
VITE_API_URL=https://zephix-backend-staging.up.railway.app/api
VITE_STRICT_JWT=false
VITE_SENTRY_ENVIRONMENT=staging
