/**
 * Canonical marketing nav for `/` — V3 only (`v3/LandingNav`).
 * Pre-V3 nav/sections are under `legacy/` and are not used here.
 */
export { LandingNav as LandingNavbar } from "./v3/LandingNav";
