Zephix Cursor Skills Index

execution-discipline
Use for plans, lanes, stop rules, minimal diffs, proof outputs.

workflow-lanes
Use to split work into lanes and block by dependency.

backend-nestjs
Use for NestJS routes, controllers, DI, guards, and backend tests.

frontend-react
Use for tabs, routing, api modules, and api client wiring.

tenancy-security
Use for workspace header, auth, csrf, role scopes, and tenant context.

testing-gates
Use for unit tests, e2e tests, gating tests, and fast pre push.

migrations-db
Use for TypeORM migrations, local Postgres, compose boot.

mcp-operations
Use for Railway and GitHub tooling via MCP and health checks.

release-staging-smoke
Use for staging validation, signup flow checks, health, version.

documentation-proofs
Use for KT, runbooks, and evidence bundles.
