# SEC-2 (PR-2) — Trivy baseline (the second database), WHOLE-REPO scope.
#
# DEBT REGISTER — read before editing. THIS FILE MAY ONLY SHRINK.
#   * Pre-existing fixable HIGH/CRITICAL from the whole-repo Trivy vuln scan
#     (matches CI: scan-ref '.', scanners: vuln). Each traces to a major-bump
#     direct in SEC-UPGRADE-1 (multer->platform-express, otel->sdk-node, ...).
#   * Also DISMISSED in the GitHub Security tab (SEC-UPGRADE-1 provenance).
#     Reopening both (this file + the dismissals) is part of SEC-UPGRADE-1 DoD.
#   * A NEW fixable HIGH/CRITICAL must fail the gate, not be added here.
#
# SHRINK LOG (a baseline that only grows is armed-but-idle in reverse):
#   2026-07-16  ROOT-CLEANUP: 33 -> 19. Removed 14 CVEs that were phantom —
#     the repo-root package.json duplicated the backend's prod deps (514
#     packages nothing imported). Those 14 CVEs were NEVER in the app.
#   2026-07-17  SEC-UPGRADE-1 unit 1 (mailer-REMOVE): 19 -> 16. Deleted
#     @nestjs-modules/mailer + nodemailer (dead deps; email via @sendgrid/mail).
#     Removed CVE-2025-14874 (nodemailer), CVE-2025-64756 (glob, vuln version gone),
#     GHSA-p6gq-j5cr-w38f (nodemailer). PROVENANCE: each mapped to its package via
#     osv.dev / GitHub advisory API, confirmed ABSENT from the post-removal lockfile
#     (nodemailer 0 occurrences; glob now 10.5.0, not the vulnerable range — npm
#     audit no longer reports it). The other 16 KEPT: multer/tar/@opentelemetry/
#     lodash — all still present, they belong to later units (otel, Nest 11).
#   2026-07-18  SEC-UPGRADE-1 unit 2 (argon2 bump 0.31.2 -> 0.44.0): 16 -> 10.
#     Removed 6 node-tar CVEs (CVE-2026-23745/23950/24842/26960/29786/31802).
#     argon2 0.31 compiled via node-gyp (pulled vulnerable tar); 0.44 ships
#     prebuilt binaries, so node-gyp/tar is gone (tar = 0 in lockfile, all nested
#     paths). PROVENANCE: GitHub advisory API (each CVE -> tar) + lockfile absence.
#     The KEPT-during-mailer-REMOVE tar entries clear HERE, not there.

CVE-2025-47935
CVE-2025-47944
CVE-2025-48997
CVE-2025-7338
CVE-2026-2359
CVE-2026-3304
CVE-2026-3520
CVE-2026-44902
CVE-2026-4800
CVE-2026-5079
