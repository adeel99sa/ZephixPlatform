# Zephix Platform - Cursor Rules

**Primary rules are in `.cursor/rules/` directory.**

See:
- `.cursor/rules/00-zephix-core.mdc` - Core engineering rules
- `.cursor/rules/10-zephix-backend.mdc` - Backend standards
- `.cursor/rules/20-zephix-frontend.mdc` - Frontend standards
- `.cursor/rules/30-zephix-security.mdc` - Security and tenancy rules

For detailed rules, refer to the `.cursor/rules/` directory.
