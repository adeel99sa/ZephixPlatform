name: build-backend
on:
  push:
    branches: [ "main" ]
    paths:
      - "zephix-backend/**"
      - ".github/workflows/backend.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      
      - name: Login to GHCR
        run: echo "${{ secrets.CR_PAT }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      
      - name: Build
        run: |
          docker build -t ghcr.io/${{ github.repository_owner }}/zephix-backend:sha-${{ github.sha }} -f zephix-backend/Dockerfile .
          docker tag ghcr.io/${{ github.repository_owner }}/zephix-backend:sha-${{ github.sha }} ghcr.io/${{ github.repository_owner }}/zephix-backend:latest
      
      - name: Push
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/zephix-backend:sha-${{ github.sha }}
          docker push ghcr.io/${{ github.repository_owner }}/zephix-backend:latest
