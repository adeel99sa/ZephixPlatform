Name
architecture-memory

Description
Use when tasks involve cross module boundaries, tenancy, auth, controller routes, migrations, staging workflows, governance, control plane concerns, or workflow lanes that can drift from platform architecture.

Trigger description keywords

cross module

tenancy

auth

controller routes

migrations

staging

workflow lanes

governance

control plane

Instructions

Read docs/ai/ARCHITECTURE_MEMORY.md first.

State which architecture memory sections apply to the current task.

Add explicit constraints from those sections into the runbook before editing.

If details are missing, locate facts in code, rules, and existing docs before making changes.

Stop if the task depends on assumptions that cannot be verified from the repository context.
