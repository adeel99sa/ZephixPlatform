# Commit set

1. `feat(tenant): require orgId-first API in TenantAwareRepository`
2. `feat(tenant): enforce scoped update/delete and strict orgId mismatch checks`
3. `chore(ci): add tenant repo call enforcement script`
4. `chore(ci): add bad rewrite detection script`
5. `refactor: update tenant repository call sites to pass orgId`
6. `fix: revert incorrect orgId injection on non-tenant repositories`
7. `docs: document tenant repository hardening and migration guide`
