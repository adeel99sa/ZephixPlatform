[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run migration:run && npm run start:prod"
# Remove health check entirely to prevent Railway from stopping the service
restartPolicyType = "always"

[env]
NODE_ENV = "production"
