/**
 * OrgInvite Role Enum
 *
 * Defines the platform roles that can be assigned via organization invites.
 * These map to PlatformRole enum values.
 */
export enum OrgInviteRole {
  ADMIN = 'admin',
  MEMBER = 'member',
  VIEWER = 'viewer',
}
