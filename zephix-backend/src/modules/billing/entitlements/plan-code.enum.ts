/**
 * Phase 3A: Plan codes for organization billing tiers.
 * No string literals outside this file.
 */
export enum PlanCode {
  FREE = 'free',
  TEAM = 'team',
  ENTERPRISE = 'enterprise',
  CUSTOM = 'custom',
}
