export enum DashboardScope {
  ORG = 'ORG',
  WORKSPACE = 'WORKSPACE',
}

export enum DashboardShareAccess {
  VIEW = 'VIEW',
  EDIT = 'EDIT',
}

export enum DashboardExportFormat {
  PDF = 'PDF',
  XLSX = 'XLSX',
}

export enum DashboardExportStatus {
  QUEUED = 'QUEUED',
  RUNNING = 'RUNNING',
  SUCCEEDED = 'SUCCEEDED',
  FAILED = 'FAILED',
}
