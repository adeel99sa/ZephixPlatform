/**
 * Units Type Enum
 * Defines the unit type for resource allocations
 *
 * PERCENT: Allocation is specified as a percentage (0-100)
 * HOURS: Allocation is specified in hours per day or week
 */
export enum UnitsType {
  PERCENT = 'PERCENT',
  HOURS = 'HOURS',
}

